import { TranformSubobjectPipe } from './tranform-subobject.pipe';

describe('TranformSubobjectPipe', () => {
  it('create an instance', () => {
    const pipe = new TranformSubobjectPipe();
    expect(pipe).toBeTruthy();
  });
});
